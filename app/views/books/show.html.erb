<main>
  <p id="notice"><%= notice %></p>

  <div class="container">
    <div class="row">
      <div class="col-md-3">
        <h2>User info</h2>

        <%= attachment_image_tag @user, :profile_image, :fill, 60, 60, fallback: "no_image.jpg" %>

        <table class="table">
          <tbody>
            <tr></tr>
            <tr>
              <th>name</th>
              <th><%= @user.name %></th>
            </tr>

            <tr>
              <th>introduction</th>
              <th><%= @user.introduction %></th>
            </tr>
          </tbody>
        </table>

        <div class="row">
          <% if @user == current_user %>
            <%= link_to "", edit_user_path(current_user), class:"btn btn-outline-secondary btn-block fas fa-user-cog" %>
          <% else %>
            <%= link_to "", user_path(current_user), class:"btn btn-outline-secondary btn-block fas fa-user-cog" %>
          <% end %>
        </div>

        <h2 class="mt-3">New book</h2>

        <%= form_with model: @book, local: true do |f| %>

        <div class="form-group">
        <%= f.label :Title %><br>
        <%= f.text_field :title, autofocus: true %>
        </div>

        <div class="form-group">
        <%= f.label :Opinion %><br />
        <%= f.text_field :body, autofocus: true %>
        </div>

        <div class="form-group">
        <%= f.submit "Create Book",class:"btn btn-success" %>
        </div>
      </div>

      <div class="col-md-8 offset-md-1">
        <h2>Book detail</h2>
        <table class="table table-hover table-inverse">
          <thead>
            <tr>
               <td>
                <% if @user == current_user %>
                  <%= link_to attachment_image_tag(@user, :profile_image, :fill, 60, 60, fallback: "no_image.jpg") + @book2.user.name, user_path(current_user) %>
                <% else %>
                  <%= link_to attachment_image_tag(@user, :profile_image, :fill, 60, 60, fallback: "no_image.jpg") + @book2.user.name, user_path(@user) %>
                <% end %>
               </td>

               <td>
                <%= link_to @book2.title, book_path(@book2) %>
               </td>

               <td>
                <%= @book2.body %>
               </td>

               <td>
                 <% if @book2.user == current_user %>
                   <%= link_to "Edit", edit_book_path(@book2), class:"btn btn-sm btn-success" %>
                 <% end %>
               </td>

               <td>
                 <% if @book2.user == current_user %>
                   <%= link_to "Destroy", book_path(@book2), method: :delete, class:"btn btn-sm btn-danger"  %>
                 <% end %>
               </td>

            </tr>
          </thead>
          <tbody></tbody>
        </table>
      </div>

      <% end %>

    </div>
  </div>
</main>

<footer>
  <div class="container mt-5">
    <div class="row">
      <div class="mx-auto">
        <p>CopyRight Infratop.inc</p>
      </div>
    </div>
  </div>
</footer>